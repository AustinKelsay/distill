# Distill Contract Test Matrix

This document is normative for planned and required contract tests.

Where a `Primary Branch` or `Target Branch` is listed below, it records the first branch that claimed the contract, even when the scenario is now implemented and passing in the current tree.

## Suite Index

| Suite                            | Purpose                                                                                                                                                         | Primary Branch                      |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| `connector_contract`             | Validate connector inputs and outputs remain within the canonical boundary.                                                                                     | `test/connector-contract-hardening` |
| `raw_capture_persistence`        | Validate Distill-owned recoverable raw capture storage.                                                                                                         | `test/raw-capture-contracts`        |
| `projection_replacement`         | Validate replace-on-success and rollback-on-failure projection semantics.                                                                                       | `test/raw-capture-contracts`        |
| `activity_audit`                 | Validate canonical audit event coverage.                                                                                                                        | `impl/activity-and-curation-audit`  |
| `search_indexing`                | Validate FTS and query behavior against the current projection.                                                                                                 | `impl/query-and-search-alignment`   |
| `session_read_model`             | Validate session detail exposes projection metadata and provenance safely.                                                                                      | `impl/projection-fidelity-export`   |
| `manual_curation`                | Validate manual tags and labels as the normative curation layer.                                                                                                | `docs/test-matrix`                  |
| `export_contract`                | Validate export payloads against the current projection and manual curation state.                                                                              | `docs/test-matrix`                  |
| `sync_jobs_and_logs`             | Validate operational sync reporting without treating logs as the canonical audit trail.                                                                         | `docs/test-matrix`                  |
| `activity_operations_read_models` | Validate cursor-paged Activity and Operations read models, redaction, and explicit cancellation states.                                                        | `feature/distill-clean-rebuild`     |
| `doc_truthfulness`               | Validate the canonical docs package stays present and wired together.                                                                                           | `docs/spec-foundation`              |
| `library_fixture_tracer`         | Validate the Rust Library Fixture ingest/projection/query/replay/health seam.                                                                                   | `feature/distill-clean-rebuild`     |
| `library_codex_source`           | Validate the Rust Library Codex detect/discover/snapshot/parse and generic Sync/replay seam.                                                                    | `feature/distill-clean-rebuild`     |
| `library_claude_source`          | Validate the Rust Library Claude Code detect/discover/snapshot/parse and generic Sync/replay seam.                                                              | `feature/distill-clean-rebuild`     |
| `library_opencode_source`        | Validate the Rust Library OpenCode virtual detect/discover/export/parse and generic Sync/replay seam.                                                           | `feature/distill-clean-rebuild`     |
| `library_droid_source`           | Validate the Rust Library Droid default/override-root detect/discover/snapshot/parse and generic Sync/replay seam.                                              | `feature/distill-clean-rebuild`     |
| `library_attempt_retry`          | Validate duplicate inertness, changed Captures, failed Attempts, renormalize, and full projection replacement.                                                  | `feature/distill-clean-rebuild`     |
| `library_parser_registry`        | Validate the closed multi-Source parser registry, Distill-owned renormalize for all five v1 kinds, and unknown-kind rejection.                                  | `feature/distill-clean-rebuild`     |
| `library_health_repair`          | Validate typed health classification, safe open reconciliation, and explicit idempotent repair.                                                                 | `feature/distill-clean-rebuild`     |
| `library_fault_injection`        | Validate ingest crash-boundary reopen states behind `--features test-faults`.                                                                                   | `feature/distill-clean-rebuild`     |
| `library_ops_sync`               | Validate Source preferences, independent detection, Sync Run concurrency/cancellation/stale leases/heartbeat/warning/partial success, and subprocess bounds.    | `feature/distill-clean-rebuild`     |
| `library_legacy_import`          | Validate WAL-safe read-only Electron-home migration, mapping, redaction, idempotency, and interrupted cleanup.                                                  | `feature/distill-clean-rebuild`     |
| `privacy_hardening`              | Validate hostile-input bounds, redaction, symlink/traversal policy, and the Tauri capability boundary.                                                          | `feature/distill-clean-rebuild`     |
| `thin_cli_fixture_caller`        | Validate the thin Rust CLI Fixture journey against a real binary.                                                                                               | `feature/distill-clean-rebuild`     |
| `thin_cli_multisource_caller`    | Validate the thin Rust CLI against Codex, Claude Code, OpenCode, and Droid product-loop, recovery, and Source-isolation journeys.                                | `feature/distill-clean-rebuild`     |
| `thin_tauri_host_fixture_caller` | Validate Tauri host input validation, progress, and typed error/result translation.                                                                             | `feature/distill-clean-rebuild`     |
| `thin_tauri_host_multisource_caller` | Validate Tauri host multi-Source configuration, product-loop durability, and provider-isolation/redaction journeys.                                      | `feature/distill-clean-rebuild`     |
| `thin_react_fixture_caller`      | Validate the React first-run UI against one typed bridge fake.                                                                                                  | `feature/distill-clean-rebuild`     |
| `thin_react_multisource_caller`  | Validate React Source preference drafts and provider-neutral multi-Source Sync/warning rendering against a typed bridge fake.                                  | `feature/distill-clean-rebuild`     |
| `thin_react_a11y_visual`         | Validate renderer keyboard/focus behavior, semantic status, contrast, scalable text, reduced motion, and deterministic state evidence.                          | `feature/distill-clean-rebuild`     |
| `macos_packaged_smoke`           | Validate the local macOS Tauri `.app` metadata, restricted capability source, packaged Fixture journey, restart persistence, and chosen-home write containment. | `feature/distill-clean-rebuild`     |
| `linux_packaged_smoke`           | Validate Ubuntu `.deb`/AppImage packaging, installed-host launch, the packaged Fixture journey, restart persistence, and chosen-home write containment.         | `feature/distill-clean-rebuild`     |
| `library_scale_budgets`          | Validate deterministic synthetic corpus targets, Library query/detail/curation p95 budgets, progress cadence, and safe-checkpoint cancellation timing.          | `feature/distill-clean-rebuild`     |
| `library_export_publication`     | Validate export eligibility, deterministic JSONL publication, checksummed bookkeeping, and restart-safe crash recovery.                                          | `feature/distill-clean-rebuild`     |

The Linux packaged smoke suite verifies emitted Debian `Depends` metadata with
`dpkg-deb` and resolves packaged controls through the AT-SPI accessible tree; it does
not claim screen-reader conformance.

## Fixture Requirements

Shared fixture requirements:

- at least one Codex live session
- at least one Codex archived duplicate of a live session
- at least one Claude Code session with mixed text and structured blocks
- at least one OpenCode export-backed virtual session
- at least one Droid session with mixed text/structured blocks and a settings sidecar
- one parse-failure fixture after successful snapshot
- one snapshot-failure fixture
- one large capture fixture that requires blob-backed persistence

Executable fixture sources:

- shared fixture manifest: `src/test/fixtures/ingest/manifest.json`
- fixture install/helper surface: `src/test/support/ingest_fixtures.ts`
- connector contract executable suite: `src/test/connector_contract.test.ts`

Every fixture must document:

- source kind
- source path or virtual path
- expected external session id
- whether it should create transcript messages
- whether it should create artifacts
- whether it should trigger failure behavior

## Scenario Matrix

| Scenario ID | Suite                             | Scenario                                                                                                                                                                                                                                  | Expected DB State                                                                                                                                                                                                                                               | Expected Query / UI Outcome                                                                                                                                        | Failure Expectations                                                                                                                          | Target Branch                        |
| ----------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| `CC-001`    | `connector_contract`              | Codex connector emits only canonical parsed shapes.                                                                                                                                                                                       | Capture parse output contains one session payload plus raw records/messages/artifacts in canonical shapes.                                                                                                                                                      | No source-specific storage logic leaks into shared layers.                                                                                                         | Test fails if connector writes non-canonical fields into shared contracts.                                                                    | `test/connector-contract-hardening`  |
| `CC-002`    | `connector_contract`              | Claude connector preserves text blocks and structured artifacts.                                                                                                                                                                          | Parsed output includes text messages plus image/tool artifacts.                                                                                                                                                                                                 | Session detail can show transcript and artifacts.                                                                                                                  | Test fails if tool/image blocks become transcript text unexpectedly.                                                                          | `test/connector-contract-hardening`  |
| `CC-003`    | `connector_contract`              | OpenCode connector preserves visible meta parts and structured artifacts.                                                                                                                                                                 | Parsed output includes messages/artifacts with canonical roles and kinds.                                                                                                                                                                                       | Session detail can show structured parts without provider leakage.                                                                                                 | Test fails if unknown structured parts are dropped.                                                                                           | `test/connector-contract-hardening`  |
| `RCP-001`   | `raw_capture_persistence`         | File-backed capture persists recoverable raw content.                                                                                                                                                                                     | Capture row resolves to a valid `CaptureContentRef` with checksum and byte size.                                                                                                                                                                                | Replay tooling can recover the original raw content.                                                                                                               | Test fails if only hashes/metadata are stored.                                                                                                | `test/raw-capture-contracts`         |
| `RCP-002`   | `raw_capture_persistence`         | Virtual OpenCode capture persists recoverable raw content.                                                                                                                                                                                | Capture row resolves to Distill-owned content, not only transient process output.                                                                                                                                                                               | Replay tooling can recover exported session JSON.                                                                                                                  | Test fails if replay depends on rerunning the source CLI.                                                                                     | `test/raw-capture-contracts`         |
| `PR-001`    | `projection_replacement`          | Exact duplicate re-import is skipped.                                                                                                                                                                                                     | No new capture row, or new row is explicitly not inserted per dedupe policy; projection rows unchanged.                                                                                                                                                         | Session list/detail/search remain unchanged.                                                                                                                       | Test fails if duplicate import mutates projection rows.                                                                                       | `test/raw-capture-contracts`         |
| `PR-002`    | `projection_replacement`          | Changed capture appends history and replaces projection.                                                                                                                                                                                  | New capture row exists; session capture count increments; messages/artifacts reflect only newest successful projection.                                                                                                                                         | Search and session detail show only current projection data.                                                                                                       | Test fails if stale message rows remain visible.                                                                                              | `test/raw-capture-contracts`         |
| `PR-003`    | `projection_replacement`          | Parse failure after snapshot preserves prior projection.                                                                                                                                                                                  | Capture exists with failure status; new capture records or projection rows are rolled back as required.                                                                                                                                                         | Existing session detail remains unchanged.                                                                                                                         | Test fails if partial rows remain.                                                                                                            | `test/raw-capture-contracts`         |
| `PR-004`    | `projection_replacement`          | Imported artifacts link directly to projected messages while retaining capture provenance.                                                                                                                                                | `artifacts.message_id` and `artifacts.capture_record_id` are both populated when a projected message association and capture provenance exist.                                                                                                                  | Session detail can show artifact/message relationships without indirect reconstruction.                                                                            | Test fails if artifact/message relationships depend on capture-record joins alone or provenance is dropped.                                   | `impl/projection-cleanup`            |
| `PR-005`    | `projection_replacement`          | Sources without stable external ids synthesize deterministic ids and record synthetic provenance.                                                                                                                                         | Session row exists with a deterministic external session id and session metadata records synthetic provenance.                                                                                                                                                  | Session detail and re-import remain stable for captures without a source-provided id.                                                                              | Test fails if projection materializes without a stable fallback id or provenance marker.                                                      | `impl/projection-cleanup`            |
| `AA-001`    | `activity_audit`                  | Successful capture and projection emit audit events.                                                                                                                                                                                      | `activity_events` includes `capture_recorded` and `projection_replaced`.                                                                                                                                                                                        | Audit views can attribute session updates to the import run.                                                                                                       | Test fails if successful import lacks canonical audit rows.                                                                                   | `impl/activity-and-curation-audit`   |
| `AA-002`    | `activity_audit`                  | Snapshot, raw-persistence, or parse failure emits canonical failure audit.                                                                                                                                                                | `activity_events` includes `capture_failed`.                                                                                                                                                                                                                    | Sync detail can show failure without mutating projection.                                                                                                          | Test fails if failures only appear in jobs/logs or abort later healthy captures.                                                              | `impl/activity-and-curation-audit`   |
| `AA-003`    | `activity_audit`                  | Manual tag and label changes emit audit rows.                                                                                                                                                                                             | `activity_events` includes `tag_added`, `tag_removed`, and `label_toggled`.                                                                                                                                                                                     | Curation history is auditable.                                                                                                                                     | Test fails if curation changes are silent.                                                                                                    | `impl/activity-and-curation-audit`   |
| `AA-004`    | `activity_audit`                  | Sync lifecycle emits canonical audit rows.                                                                                                                                                                                                | `activity_events` includes `sync_queued`, `sync_started`, `sync_completed`, or `sync_failed`.                                                                                                                                                                   | Audit and ops summaries can be reconciled.                                                                                                                         | Test fails if sync lifecycle is visible only through jobs.                                                                                    | `impl/activity-and-curation-audit`   |
| `AOR-001`   | `activity_operations_read_models` | Activity is append-only and caller-paged newest-first.                                                                                                                                                                                    | `list_activity` uses deterministic opaque keyset cursors, preserves safe nested reason/status/metrics fields, and redacts paths/SQL/provider payloads without rewriting rows.                                                                                   | CLI, host, and renderer can replay pages and show domain history independently of log cleanup.                                                                     | Test fails if pages overlap, payload privacy leaks, malformed payloads break reads, or operational cleanup changes Activity.                  | `feature/distill-clean-rebuild`      |
| `AOR-002`   | `activity_operations_read_models` | Operations diagnostics remain separate from Activity.                                                                                                                                                                                     | `list_operations` returns `operations_status`, warning/failed/cancelled/stale Sync Run summaries, and independent export lifecycle pages; paths are omitted/redacted.                                                                                           | Operators can understand current and historical execution without treating logs as domain history.                                                                 | Test fails if operations mutate Activity, export paths leak, or warning/cancelled/export lifecycle states disappear.                          | `feature/distill-clean-rebuild`      |
| `AOR-003`   | `activity_operations_read_models` | Thin callers expose stable diagnostics states and JSON.                                                                                                                                                                                   | CLI invalid-cursor/usage/runtime exits are stable; Tauri/React surfaces provide explicit loading, empty, warning, error, and user-cancelled states with typed bridge calls.                                                                                     | Desktop and automation callers can distinguish no data, partial health, failure, and cancellation.                                                                 | Test fails if diagnostics fetch ambiently, cancelled requests update after invalidation, or JSON shape/exit code drifts.                      | `feature/distill-clean-rebuild`      |
| `SI-001`    | `search_indexing`                 | Search returns current projected transcript rows only.                                                                                                                                                                                    | FTS rows correspond to current message projection.                                                                                                                                                                                                              | Search results exclude stale superseded rows.                                                                                                                      | Test fails if replaced messages remain searchable.                                                                                            | `impl/query-and-search-alignment`    |
| `SI-002`    | `search_indexing`                 | Search safely handles punctuation-heavy and zero-token queries.                                                                                                                                                                           | No DB corruption or invalid FTS query state.                                                                                                                                                                                                                    | Results still resolve for quoted and dashed input, and all-non-token input returns no results.                                                                     | Test fails if queries crash, over-match, or treat zero-token input as a broad query.                                                          | `impl/query-and-search-alignment`    |
| `SI-003`    | `search_indexing`                 | Current-projection session search uses deterministic cursor pages and intersects workflow lanes.                                                                                                                                          | One row per current Session Projection; `(updated_at, session_id)` keyset cursor has no duplicates or omissions; only manual labels drive lanes.                                                                                                                | CLI, host, and renderer can refresh a stable selected row while loading more.                                                                                      | Test fails if superseded text leaks, cursor ties reorder, or non-manual labels change lane membership.                                        | `feature/distill-clean-rebuild`      |
| `SRM-001`   | `session_read_model`              | Session detail exposes stored projection metadata safely.                                                                                                                                                                                 | Session row fields remain readable and malformed legacy `metadata_json` reads back as `{}`.                                                                                                                                                                     | Session detail includes external session id, timestamps, source URL, summary, raw capture count, and parsed session metadata from the current projection.          | Test fails if session detail hides projection metadata or bad JSON breaks the read model.                                                     | `impl/projection-fidelity-export`    |
| `SRM-002`   | `session_read_model`              | Session detail slices current messages and artifacts with continuation cursors and manual curation read models.                                                                                                                           | Bounded message/artifact pages expose current generation only; tags/labels include origin and exclude non-manual assignments.                                                                                                                                   | Desktop and CLI can render large transcripts incrementally without ambiguous counts.                                                                               | Test fails if stale rows, raw-capture history, or derived/non-manual labels appear in the detail read model.                                  | `feature/distill-clean-rebuild`      |
| `MC-001`    | `manual_curation`                 | Manual tags appear in session detail and export.                                                                                                                                                                                          | Tag rows and assignments exist with manual origin.                                                                                                                                                                                                              | Session detail and export payloads agree.                                                                                                                          | Test fails if export and detail diverge.                                                                                                      | `feature/distill-clean-rebuild`      |
| `MC-002`    | `manual_curation`                 | Manual labels remain session-level only.                                                                                                                                                                                                  | Label assignments target sessions and preserve origin.                                                                                                                                                                                                          | Dataset-export behavior matches session detail state, and query read models ignore non-manual label origins.                                                       | Test fails if label scope drifts silently.                                                                                                    | `feature/distill-clean-rebuild`      |
| `MC-003`    | `manual_curation`                 | Enabling a dataset label removes conflicting dataset labels and audits both transitions.                                                                                                                                                  | Only one of `train`, `holdout`, or `exclude` remains assigned after the toggle; audit rows capture the disable and enable events.                                                                                                                               | Session detail and workflow state reflect the winning dataset label immediately.                                                                                   | Test fails if conflicting dataset labels coexist after a manual enable or if the automatic removal is silent.                                 | `feature/distill-clean-rebuild`      |
| `MC-004`    | `manual_curation`                 | Review-only labels preserve orthogonal labels while taking workflow priority.                                                                                                                                                             | `favorite` and `sensitive` can coexist with one dataset label, and `exclude` removes only conflicting dataset labels.                                                                                                                                           | `Needs Review` shows `exclude`, `sensitive`, and conflicting dataset-label sessions; `Favorites` still includes favorite sessions.                                 | Test fails if orthogonal labels are dropped, conflicting dataset labels surface as export-ready, or workflow priority is wrong.               | `feature/distill-clean-rebuild`      |
| `MC-005`    | `manual_curation`                 | Session curation commands are normalized, idempotent, and transactionally audited.                                                                                                                                                        | Tag/label mutations return a current manual snapshot; blank/unknown/duplicate/missing commands are no-ops; changed assignments and Activity Events commit together.                                                                                             | CLI, host, and renderer update immediately from one typed Library outcome.                                                                                         | Test fails if a no-op emits Activity, a dataset switch drops modifiers, or the read model can observe a partial mutation.                     | `feature/distill-clean-rebuild`      |
| `EC-001`    | `export_contract`                 | Export uses current session projection, not raw history.                                                                                                                                                                                  | Export bookkeeping row exists; payload matches current projection.                                                                                                                                                                                              | Exported messages equal current session detail transcript.                                                                                                         | Test fails if superseded rows appear in output.                                                                                               | `docs/test-matrix`                   |
| `EC-002`    | `export_contract`                 | Export includes manual curation metadata.                                                                                                                                                                                                 | Export row and output include tags and labels.                                                                                                                                                                                                                  | Consumers can trust export metadata without re-querying Distill.                                                                                                   | Test fails if tags/labels are missing or inconsistent.                                                                                        | `docs/test-matrix`                   |
| `EC-003`    | `export_contract`                 | Export preserves projection metadata and per-message transcript semantics.                                                                                                                                                                | Export payload includes session metadata plus message kind and message metadata from the current projection.                                                                                                                                                    | Consumers can distinguish text from meta messages, derive turn pairs from real assistant replies only, and recover session provenance without re-querying Distill. | Test fails if export drops session metadata, collapses `message_kind`, pairs turns from assistant meta rows, or omits message metadata.       | `impl/projection-fidelity-export`    |
| `EC-004`    | `export_contract`                 | Standard dataset export excludes review-only or conflicting dataset-label sessions.                                                                                                                                                       | Export rows are written for `train` and `holdout` targets only, and sessions with `exclude`, `sensitive`, or conflicting dataset labels are omitted even when they carry a matching dataset label.                                                              | Operators get safe-by-default dataset exports from the main UI.                                                                                                    | Test fails if `exclude`, `sensitive`, or conflicting dataset-label sessions appear in standard dataset export.                                | `impl/curation-policy-export-safety` |
| `EC-005`    | `export_contract`                 | Favorite sessions remain exportable only through their dataset label.                                                                                                                                                                     | A session labeled `favorite` plus `train` or `holdout` exports with both labels preserved in metadata.                                                                                                                                                          | `Favorites` remains an organizational lane, not an export target.                                                                                                  | Test fails if favorite-only sessions export or if favorite disappears from exported metadata.                                                 | `impl/curation-policy-export-safety` |
| `SL-001`    | `sync_jobs_and_logs`              | Sync job summaries remain operational, not canonical audit.                                                                                                                                                                               | Job rows contain sync status and metrics.                                                                                                                                                                                                                       | Logs show sync state while audit remains the source of truth.                                                                                                      | Test fails if log behavior depends on missing audit guarantees.                                                                               | `docs/test-matrix`                   |
| `SL-002`    | `sync_jobs_and_logs`              | Export summaries remain visible in logs.                                                                                                                                                                                                  | Export bookkeeping is preserved.                                                                                                                                                                                                                                | Logs show operational export summaries.                                                                                                                            | Test fails if export operations disappear from ops surfaces.                                                                                  | `docs/test-matrix`                   |
| `SL-003`    | `sync_jobs_and_logs`              | Warning-only syncs remain visible as non-fatal warnings.                                                                                                                                                                                  | Job/log surfaces preserve `status = "warning"` plus sync metrics and warning details without flipping canonical audit.                                                                                                                                          | Operators can distinguish partial success from fatal sync failure.                                                                                                 | Test fails if warning-only syncs are treated as errors or disappear from ops surfaces.                                                        | `docs/test-matrix`                   |
| `DT-001`    | `doc_truthfulness`                | Canonical docs package exists and is linked from root docs.                                                                                                                                                                               | Required markdown files exist.                                                                                                                                                                                                                                  | Contributors can navigate from root docs to canonical docs.                                                                                                        | Test fails if a required canonical doc is removed or unlinked.                                                                                | `docs/spec-foundation`               |
| `DT-002`    | `doc_truthfulness`                | Root docs remain non-authoritative summaries.                                                                                                                                                                                             | Root docs contain the required links and disclaimers.                                                                                                                                                                                                           | Readers are directed to `docs/` for canonical truth.                                                                                                               | Test fails if root docs re-assume canonical authority.                                                                                        | `docs/spec-foundation`               |
| `LFT-001`   | `library_fixture_tracer`          | Fresh Distill home bootstraps with checksummed migrations and restrictive Unix modes.                                                                                                                                                     | `schema_migrations` rows exist with matching checksums; home dirs are `0o700` and db/files are `0o600`.                                                                                                                                                         | Library opens and reports healthy schema status.                                                                                                                   | Test fails if legacy Electron schema is applied or modes are permissive.                                                                      | `feature/distill-clean-rebuild`      |
| `LFT-002`   | `library_fixture_tracer`          | Fixture SourceAdapter detect/discover/snapshot/parse through production ingest yields Capture, Attempt, Facts, Projection, FTS, and Activity.                                                                                             | Capture row and `capture_recorded` Activity commit together over recoverable content; successful Attempt has Facts; Session Projection and FTS match.                                                                                                           | `session_slice` and bounded `search` return the projected transcript.                                                                                              | Test fails if Fixture bypasses the SourceAdapter seam or projection publishes without an Attempt.                                             | `feature/distill-clean-rebuild`      |
| `LFT-003`   | `library_fixture_tracer`          | Blob-backed replay after source deletion and Library reopen remains checksum-verified.                                                                                                                                                    | A Capture above the inline threshold resolves from Distill-owned content-addressed storage after fixture files are removed.                                                                                                                                     | `replay_capture` and `health` succeed after reopen.                                                                                                                | Test fails if replay depends on the original Fixture files or only exercises inline storage.                                                  | `feature/distill-clean-rebuild`      |
| `LFT-004`   | `library_fixture_tracer`          | Symlink and missing-parent configured-root escapes plus capture-size limits fail with typed errors.                                                                                                                                       | No Capture row is accepted for rejected candidates.                                                                                                                                                                                                             | Callers observe `PathOutsideConfiguredRoot` before snapshot or `CaptureTooLarge` before acceptance.                                                                | Test fails if escapes or oversized bytes are accepted or misclassified.                                                                       | `feature/distill-clean-rebuild`      |
| `LFT-005`   | `library_fixture_tracer`          | `run_fixture_journey` returns source, sync, session, and health with typed progress phases.                                                                                                                                               | Journey result carries detected Fixture Source, ingest counters/Session Identities, projected Session, and healthy report.                                                                                                                                      | Thin callers can render the first-run outcome without SQL.                                                                                                         | Test fails if progress phases are skipped or storage handles leak into the result.                                                            | `feature/distill-clean-rebuild`      |
| `LCS-001`   | `library_codex_source`            | Codex detection and discovery report configured roots, ignore auxiliary index/history files, and prefer live over archived duplicates by provider Session Identity.                                                                       | One live candidate remains for a duplicated Session Identity; logical `codex://` paths are persisted.                                                                                                                                                           | Generic Source and Candidate progress is safe for CLI/desktop callers.                                                                                             | Test fails if archive wins, discovery is filesystem-order dependent, or absolute paths leak.                                                  | `feature/distill-clean-rebuild`      |
| `LCS-002`   | `library_codex_source`            | Codex snapshot preserves exact JSONL bytes and replay remains available after source deletion.                                                                                                                                            | Capture content checksum/byte size matches the original source bytes.                                                                                                                                                                                           | `replay_capture` returns the same bytes after the Codex home is removed.                                                                                           | Test fails if replay rereads the provider root or stores only parsed JSON.                                                                    | `feature/distill-clean-rebuild`      |
| `LCS-003`   | `library_codex_source`            | Codex dialogue, metadata, tool/reasoning/unknown-role rows, instruction filtering, and synthetic identity project through the shared Library shapes.                                                                                      | Current Session Projection has only visible user/assistant messages; facts/artifacts retain provider rows and provenance.                                                                                                                                       | Search/session detail expose canonical transcript and structured artifacts.                                                                                        | Test fails if provider records become caller-specific fields or instruction noise becomes visible transcript.                                 | `feature/distill-clean-rebuild`      |
| `LCS-004`   | `library_codex_source`            | Codex SourceAdapter preserves dialogue, provider facts/artifacts, synthetic provenance, and typed stage errors.                                                                                                                           | Codex parse output remains canonical and raw rows stay replayable.                                                                                                                                                                                              | Session detail exposes transcript and structured artifacts without Codex policy in shared shapes.                                                                  | Test fails if source bytes are normalized before snapshot or stage errors lose their type.                                                    | `feature/distill-clean-rebuild`      |
| `LCL-001`   | `library_claude_source`           | Claude Code detection/discovery finds project JSONL, excludes history/settings, and keeps logical source paths and deterministic identity order.                                                                                          | One candidate per resolved identity with canonical `claude://project/...` path.                                                                                                                                                                                 | Generic Source/Candidate progress is safe for CLI/desktop callers.                                                                                                 | Test fails if auxiliary files become Captures or absolute paths leak.                                                                         | `feature/distill-clean-rebuild`      |
| `LCL-002`   | `library_claude_source`           | Claude Code snapshot preserves exact bytes and replay survives source removal.                                                                                                                                                            | Capture checksum/byte size matches source JSONL.                                                                                                                                                                                                                | `replay_capture` succeeds after deleting the Claude home.                                                                                                          | Test fails if replay rereads provider files.                                                                                                  | `feature/distill-clean-rebuild`      |
| `LCL-003`   | `library_claude_source`           | Claude Code mixed text blocks, images, tool calls/results, files, reasoning, unknown roles, metadata, suppression, and synthetic identity map to canonical facts/messages/artifacts.                                                      | Current projection contains visible user/assistant text; structured blocks retain capture-fact provenance.                                                                                                                                                      | Session detail/search show canonical transcript and structured artifacts without provider fields.                                                                  | Test fails if meta/noise becomes transcript or structured blocks are dropped.                                                                 | `feature/distill-clean-rebuild`      |
| `LCL-004`   | `library_claude_source`           | Unreadable project roots and malformed JSONL return typed discovery/parse failures through generic Sync.                                                                                                                                  | Failed Source outcome carries redacted `source_adapter` diagnostics and no partial projection.                                                                                                                                                                  | Callers render a generic failed Source without path/payload leakage.                                                                                               | Test fails if discovery silently drops errors or caller diagnostics expose the root.                                                          | `feature/distill-clean-rebuild`      |
| `LOC-001`   | `library_opencode_source`         | OpenCode detection and virtual discovery use a configured root, bounded executable, stable `opencode://session/<id>` identity, and generic missing-executable diagnostics.                                                                | Empty healthy roots complete without captures; unavailable executables remain typed and redacted.                                                                                                                                                               | CLI/desktop callers receive generic Source/Candidate progress.                                                                                                     | Test fails if discovery is unbounded, identity is absolute, or provider paths leak.                                                           | `feature/distill-clean-rebuild`      |
| `LOC-002`   | `library_opencode_source`         | OpenCode snapshot preserves the complete export stdout payload, including leading export text, and replay survives source removal.                                                                                                        | Capture checksum/byte size matches exact exported bytes stored in Distill-owned content.                                                                                                                                                                        | `replay_capture` returns the same bytes after the OpenCode root is removed.                                                                                        | Test fails if replay reruns the CLI or stores only parsed JSON.                                                                               | `feature/distill-clean-rebuild`      |
| `LOC-003`   | `library_opencode_source`         | OpenCode dialogue, reasoning, tools/results, files, unknown roles, model metadata, titles, timestamps, and unknown structured parts map to canonical facts/messages/artifacts.                                                            | Current projection contains visible text and structured artifact provenance; metadata retains provider details without provider-specific shared fields.                                                                                                         | Session detail/search expose canonical transcript and artifacts.                                                                                                   | Test fails if unknown structured parts are dropped or provider policy leaks into Library shapes.                                              | `feature/distill-clean-rebuild`      |
| `LOC-004`   | `library_opencode_source`         | OpenCode timeout, stdout overflow, command failure, malformed export, and synthetic identity paths remain typed and redacted.                                                                                                             | Failed Source outcome has stable `source_adapter` diagnostics and no partial projection; synthetic ids are deterministic.                                                                                                                                       | Callers render a generic failed Source and can retry from owned bytes.                                                                                             | Test fails if process cleanup is incomplete, diagnostics expose payloads, or invalid timestamps crash parsing.                                | `feature/distill-clean-rebuild`      |
| `LDR-001`   | `library_droid_source`            | Droid detection honors configured override, stored preference, and `$HOME/.factory/sessions` default roots; recursive discovery excludes settings sidecars and deduplicates deterministic logical identities.                             | Healthy, absent, disabled, and unreadable roots remain independently typed; candidates use `droid://session/<id>` without absolute paths.                                                                                                                       | CLI/desktop callers receive generic Source/Candidate progress and stable first-wins discovery.                                                                     | Test fails if default/override precedence, recursion, sidecar exclusion, duplicate resolution, or redaction drifts.                           | `feature/distill-clean-rebuild`      |
| `LDR-002`   | `library_droid_source`            | Droid snapshot preserves exact JSONL bytes, SHA-256, byte size, and source metadata; replay survives session/root deletion.                                                                                                               | Capture content checksum/byte size matches the original file and Distill-owned content remains recoverable.                                                                                                                                                     | `replay_capture` returns identical bytes after the Droid root is removed.                                                                                          | Test fails if replay rereads the provider root or snapshot stores only parsed JSON.                                                           | `feature/distill-clean-rebuild`      |
| `LDR-003`   | `library_droid_source`            | Droid session-start metadata, sidecar title/model/archive fields, project/owner values, timestamps, string/structured mixed content, visible dialogue, unknown roles, and structured artifacts map to canonical facts/messages/artifacts. | Current projection contains only visible user/assistant text; facts/artifacts retain provider structure and provenance.                                                                                                                                         | Session detail/search expose canonical transcript and artifacts without Droid policy fields in shared shapes.                                                      | Test fails if unknown blocks/roles are dropped, thinking/tool/image/file content becomes transcript text, or metadata is lost.                | `feature/distill-clean-rebuild`      |
| `LDR-004`   | `library_droid_source`            | Droid malformed JSON/UTF-8 inputs remain typed and redacted while stem and session-start identity precedence stays deterministic; adapter unit coverage proves deterministic synthetic provenance for no-id candidates.                   | Failed Sync records a recoverable Capture with safe parse failure; adapter synthetic identity remains stable.                                                                                                                                                   | Callers render generic warning/failure outcomes and retry from owned bytes.                                                                                        | Test fails if malformed bytes panic, diagnostics leak paths/payloads, or identity changes across replays.                                     | `feature/distill-clean-rebuild`      |
| `AR-001`    | `library_attempt_retry`           | Exact duplicate Fixture re-ingest is inert.                                                                                                                                                                                               | No new Capture, Attempt, projection Activity, or FTS change.                                                                                                                                                                                                    | Session counters and transcript remain unchanged.                                                                                                                  | Test fails if duplicate ingest mutates Capture/Attempt/projection/Activity/FTS.                                                               | `feature/distill-clean-rebuild`      |
| `AR-002`    | `library_attempt_retry`           | Changed bytes append a Capture and replace the projection.                                                                                                                                                                                | New Capture and Attempt exist; superseded transcript leaves FTS.                                                                                                                                                                                                | Session detail shows only the newest successful projection.                                                                                                        | Test fails if stale messages remain visible or searchable.                                                                                    | `feature/distill-clean-rebuild`      |
| `AR-003`    | `library_attempt_retry`           | Parse or projection failure records a typed safe Attempt and preserves last-good projection/FTS.                                                                                                                                          | Failed Attempt has `parse_failed` or `projection_failed`; prior generation unchanged.                                                                                                                                                                           | Session counters update honestly without projection mutation.                                                                                                      | Test fails if last-good rows change or diagnostics are missing/unsafe.                                                                        | `feature/distill-clean-rebuild`      |
| `AR-004`    | `library_attempt_retry`           | Strictly newer registered Fixture parser renormalizes the same Capture from Distill-owned bytes.                                                                                                                                          | No new Capture; prior successful Attempts/Facts remain immutable; successful retry publishes a projection.                                                                                                                                                      | `capture_attempts` exposes history and Fact counts; duplicate re-ingest stays inert.                                                                               | Test fails if retry invents a Capture, mutates prior Attempts/Facts, permits a non-advancing version, or accepts arbitrary caller parser ids. | `feature/distill-clean-rebuild`      |
| `AR-005`    | `library_attempt_retry`           | Successful shorter and empty projections fully replace prior slices.                                                                                                                                                                      | Current projection message/artifact sets match the newest successful Attempt only.                                                                                                                                                                              | Search excludes cleared superseded text; CLI/UI expose separately named Capture, Attempt, and generation counters.                                                 | Test fails if old rows leak or counters are collapsed into one ambiguous field.                                                               | `feature/distill-clean-rebuild`      |
| `LPR-001`   | `library_parser_registry`         | Typed `set_registered_parser_version` advances each closed Source kind independently; Fixture shim remains compatible; callers never supply parser ids.                                                                                    | Registry versions are Source-specific; non-advancing/malformed versions are rejected.                                                                                                                                                                           | Detect/ingest record adapter-owned parser ids with the registered versions.                                                                                        | Test fails if a kind shares another kind's version, the Fixture shim changes parser id, or arbitrary parser ids are accepted.                 | `feature/distill-clean-rebuild`      |
| `LPR-002`   | `library_parser_registry`         | Fixture, Codex, Claude Code, OpenCode, and Droid Captures renormalize after Source-root removal using Distill-owned bytes only (OpenCode without subprocess).                                                                             | No new Capture; prior Attempts immutable; newer registered versions append Attempts and replace Projection.                                                                                                                                                     | Replay/search remain available after root removal.                                                                                                                 | Test fails if renormalize rereads a removed root, invokes OpenCode, mutates prior Attempts, or skips a closed kind.                           | `feature/distill-clean-rebuild`      |
| `LPR-003`   | `library_parser_registry`         | Parse and projection failures on renormalize preserve last-good Projection; successful empty Projection fully clears messages/artifacts.                                                                                                  | Failed Attempts keep typed diagnostics; empty success deletes prior projection rows.                                                                                                                                                                            | Prior successful Fixture projection remains searchable until empty replacement.                                                                                    | Test fails if failure mutates last-good Projection or empty success leaves stale messages/artifacts.                                          | `feature/distill-clean-rebuild`      |
| `LPR-004`   | `library_parser_registry`         | Unknown/unregistered persisted Source kinds return typed redacted `UnknownSourceKind` without Attempt or Projection mutation.                                                                                                              | Attempt history and Projection remain unchanged.                                                                                                                                                                                                                | Error code is `unknown_source_kind` and contains no path dumps.                                                                                                    | Test fails if unknown kind invents Attempts, mutates Projection, or leaks filesystem paths.                                                   | `feature/distill-clean-rebuild`      |
| `LHR-001`   | `library_health_repair`           | Healthy Fixture home reports ok across schema/content/FTS/staging/orphan/incomplete plus Sync operations.                                                                                                                                 | Typed `HealthReport` statuses are all `ok` with `operations_status=ok` and empty issues.                                                                                                                                                                        | Callers can trust a green health panel after ordinary ingest.                                                                                                      | Test fails if category statuses are missing or falsely failed.                                                                                | `feature/distill-clean-rebuild`      |
| `LHR-002`   | `library_health_repair`           | Safe open removes only canonical `{64 lowercase hex}.partial` staging files and reports the count.                                                                                                                                        | `OpenReconciliation.removed_staging_partials` matches deleted canonical partials; noncanonical `*.partial` remain and are reported as `unrecognized_staging_entry`.                                                                                             | Reopen leaves no canonical staging partials and does not delete CAS blobs or junk staging names.                                                                   | Test fails if open deletes orphans/referenced content/noncanonical partials or omits reconciliation counts.                                   | `feature/distill-clean-rebuild`      |
| `LHR-003`   | `library_health_repair`           | Unreferenced CAS blobs are repairable only via explicit repair; repair is idempotent; CAS symlinks/malicious `blob_path` never touch external targets.                                                                                    | Health reports `orphan_blob` / `cas_unrecognized_entry` / `content_invalid_blob_path`; first repair removes only in-root regular canonical orphans; external symlink targets survive.                                                                           | Open never silently deletes orphans; health/repair never follow symlinks or leave the Distill home.                                                                | Test fails if open deletes orphans, repair deletes external targets, or summaries leak raw paths.                                             | `feature/distill-clean-rebuild`      |
| `LHR-004`   | `library_health_repair`           | Projection/FTS disagreement covers every searchable field and is rebuildable from Session title/project_path.                                                                                                                             | Tampered FTS text/title/project_path fails health; `rebuild_fts` restores agreement from Session rows.                                                                                                                                                          | Count-only equality is insufficient.                                                                                                                               | Test fails if health passes on title/path mismatch or rebuild hardcodes empty project_path.                                                   | `feature/distill-clean-rebuild`      |
| `LHR-005`   | `library_health_repair`           | Incomplete Captures and pending Attempts are classified and resolved without inventing Attempts; empty successful projections stay healthy; counter drift is repaired.                                                                    | Health reports `incomplete_capture` / `pending_attempt` / `session_counter_mismatch`; repair fails pending Attempts, appends Capture-scoped `capture_failed`, recomputes counters, preserves last-good generation; metadata-only empty projections remain `ok`. | Repeated repair is idempotent; exact duplicates stay inert.                                                                                                        | Test fails if repair invents `parser_id=repair` Attempts, empty projections are marked incomplete, or counters stay stale.                    | `feature/distill-clean-rebuild`      |
| `LHR-006`   | `library_health_repair`           | Missing referenced content is blocking and not deleted/fixed by repair.                                                                                                                                                                   | Health reports `content_missing`; repair leaves content status failed.                                                                                                                                                                                          | Operators see integrity failure without silent data loss.                                                                                                          | Test fails if repair claims success or deletes Captures.                                                                                      | `feature/distill-clean-rebuild`      |
| `LHR-007`   | `library_health_repair`           | Operations status reports `ok` with no invented stale codes when no Sync Run is active; active/stale coverage lives in `OSR-*`.                                                                                                           | `operations_status=ok`; no `stale_sync_operation` / sync-category issues on a clean home.                                                                                                                                                                       | Sync health is real typed state, not omitted.                                                                                                                      | Test fails if Sync category is silently absent or invents stale codes on a clean home.                                                        | `feature/distill-clean-rebuild`      |
| `LMI-001`   | `library_legacy_import`           | A legacy Electron home is imported through a private read-only SQLite snapshot; rollback-journal and WAL homes remain byte-for-byte unchanged.                                                                                            | Source database/sidecars and in-home content digests are unchanged; source paths are never opened for writes.                                                                                                                                                   | Migration is safe while preserving the legacy evidence boundary.                                                                                                   | Test fails if a WAL `-shm`/`-wal` file is created or changed in the source home.                                                              | `feature/distill-clean-rebuild`      |
| `LMI-002`   | `library_legacy_import`           | Representative legacy Captures, Facts, Sessions, projections, tags, labels, Activity, and export metadata map into native Library read models.                                                                                            | Checksums, search, curation, Activity, and export bytes/metadata agree; unsupported or unsafe rows produce stable skip codes.                                                                                                                                   | Operators can validate migrated data without SQL access.                                                                                                           | Test fails if raw content is not recoverable, FTS is stale, or report/durable metadata leaks paths or SQL.                                    | `feature/distill-clean-rebuild`      |
| `LMI-003`   | `library_legacy_import`           | Same source fingerprints reuse a durable marker; destination SQL failure cleans only import-owned unreferenced CAS/export files.                                                                                                          | Repeated import reports `reused_prior_import`; interrupted import leaves no unreferenced file created by that attempt.                                                                                                                                          | Retry is idempotent and recoverable.                                                                                                                               | Test fails if rows duplicate, marker is missing, or cleanup deletes pre-existing content.                                                     | `feature/distill-clean-rebuild`      |
| `LMI-004`   | `library_legacy_import`           | Same/alias/ancestor homes, traversal, symlink, missing, checksum-mismatched, and unsupported legacy paths are rejected or skipped without source mutation.                                                                                | Errors and skip reports are typed, stable, and redacted.                                                                                                                                                                                                        | Host/CLI/renderer callers can render safe first-run migration outcomes.                                                                                            | Test fails if an external file is read/written or a report leaks a raw path/payload.                                                          | `feature/distill-clean-rebuild`      |
| `LPH-001`   | `privacy_hardening`               | Fixture traversal, symlink escapes, oversized Captures, and malformed UTF-8 fail before false acceptance.                                                                                                                                 | No escaped or oversized Capture is accepted; malformed bytes produce a typed failed ingest with no Session Projection.                                                                                                                                          | Library/CLI/Tauri callers receive stable redacted errors without raw source paths.                                                                                 | Test fails if a symlink escape is read, a large file is loaded before rejection, or invalid bytes become a Capture.                           | `feature/distill-clean-rebuild`      |
| `LPH-002`   | `privacy_hardening`               | Every file-backed adapter skips discovery symlinks and rejects deep/oversized JSONL; HTML/script payloads remain literal transcript data.                                                                                                 | Codex, Claude Code, Droid, and Fixture parse failures record failed Attempts; literal script text is preserved as text only.                                                                                                                                    | Search/session detail do not execute or render provider markup.                                                                                                    | Test fails if deep/oversized JSON publishes a Projection or script text is treated as markup.                                                 | `feature/distill-clean-rebuild`      |
| `LPH-003`   | `privacy_hardening`               | OpenCode provider timeout/output/large-stdin bounds and malformed exports remain typed and redacted.                                                                                                                                      | `library_opencode_source` / `library_ops_sync` report bounded `provider_process_bound_exceeded` or parse failures without raw argv/payload.                                                                                                                     | Operations and CLI/Tauri diagnostics retain only stable error context.                                                                                             | Test fails if a provider can run unbounded, deadlock stdin, or leak command/output text.                                                      | `feature/distill-clean-rebuild`      |
| `LPH-004`   | `privacy_hardening`               | Activity, Operations, migration, CLI, and Tauri diagnostics redact paths, SQL, command/output, provider payloads, and secret-shaped values.                                                                                               | Durable/read-model payloads retain safe metrics/reasons while removing raw sensitive fields; malformed JSON reads as `{}`.                                                                                                                                      | Callers never receive raw paths, credentials, SQL, or provider payloads.                                                                                           | Test fails if any caller-facing error or Activity/Operations field contains a raw secret/path/payload.                                        | `feature/distill-clean-rebuild`      |
| `LPH-005`   | `privacy_hardening`               | Tauri host validates renderer paths/enums/identifiers and the default capability grants only core events.                                                                                                                                 | Parent traversal, NUL, missing Fixture roots, invalid dataset/id values return typed validation; capabilities contain no FS/shell/process/SQL/plugin grants.                                                                                                    | Renderer has no ambient storage or process authority.                                                                                                              | Test fails if a path is echoed, an unapproved command/plugin is granted, or invalid host input reaches Library work.                          | `feature/distill-clean-rebuild`      |
| `LPH-006`   | `privacy_hardening`               | Renderer bridge uses exact typed `invoke`/`listen` calls and cleans up listeners; v1 privacy scope is explicit.                                                                                                                           | Bridge tests cover command payloads, listener race cleanup, and deny-list imports; docs state export-only `sensitive` plus no encryption/delete/purge/secure-forget.                                                                                            | UI receives typed host results/events only.                                                                                                                        | Test fails if ambient APIs appear, late listeners leak, or the documented v1 boundary is omitted.                                             | `feature/distill-clean-rebuild`      |
| `OSR-001`   | `library_ops_sync`                | Source preferences persist enabled/disabled and canonical configured roots.                                                                                                                                                               | Preference rows survive reopen; roots are canonical directories.                                                                                                                                                                                                | Callers can enable Fixture with an override root.                                                                                                                  | Test fails if empty/traversal roots are accepted or preferences are lost.                                                                     | `feature/distill-clean-rebuild`      |
| `OSR-002`   | `library_ops_sync`                | Detection returns independent typed results; one failure never aborts siblings.                                                                                                                                                           | Mixed Fixture ok/unhealthy plus unavailable provider kinds coexist in one response.                                                                                                                                                                             | Operators see per-Source health without aborting the batch.                                                                                                        | Test fails if one bad Source suppresses healthy results.                                                                                      | `feature/distill-clean-rebuild`      |
| `OSR-003`   | `library_ops_sync`                | Overlapping Sync from a second Library returns `sync_already_running` with no durable side effects.                                                                                                                                       | No extra Sync Run or Activity rows from the rejected start.                                                                                                                                                                                                     | Concurrency cannot interleave Sync bookkeeping.                                                                                                                    | Test fails if overlap creates Sync/Activity rows or relies on a process-global mutex.                                                         | `feature/distill-clean-rebuild`      |
| `OSR-004`   | `library_ops_sync`                | Cancel from a second Library honors the next candidate checkpoint.                                                                                                                                                                        | First candidate may finish; later candidates are not accepted; run status `cancelled`.                                                                                                                                                                          | Safe cancel never interrupts Capture/Attempt/projection transactions.                                                                                              | Test fails if cancel aborts mid-candidate or ignores durable cancel requests.                                                                 | `feature/distill-clean-rebuild`      |
| `OSR-005`   | `library_ops_sync`                | Successful Sync completes with Activity and projection.                                                                                                                                                                                   | `sync_queued`/`sync_started`/`sync_completed` exist; Captures/Sessions projected.                                                                                                                                                                               | CLI/host can trust Sync completion.                                                                                                                                | Test fails if Sync skips Activity or projection.                                                                                              | `feature/distill-clean-rebuild`      |
| `OSR-006`   | `library_ops_sync`                | Live leases report `active`; stale leases fail idempotently on reopen; failed worker cannot overwrite or accept post-stale Captures.                                                                                                      | Observer health sees `active` for live leases; aging durable `lease_expires_at` then reopening fails stale once; joined worker returns `sync_lease_lost`; final status stays `failed` with one `sync_failed`.                                                   | Restart semantics are accurate without injectable clocks.                                                                                                          | Test fails if live leases are marked stale, reopen duplicates `sync_failed`, or the old worker overwrites terminal state.                     | `feature/distill-clean-rebuild`      |
| `OSR-007`   | `library_ops_sync`                | Configured-root empty/traversal rejected; symlink roots canonicalize.                                                                                                                                                                     | Invalid roots raise `invalid_configured_root`; symlink stores canonical target.                                                                                                                                                                                 | Path policy is explicit and tested.                                                                                                                                | Test fails if escapes are accepted or symlink policy is undefined.                                                                            | `feature/distill-clean-rebuild`      |
| `OSR-008`   | `library_ops_sync`                | Provider subprocess output and duration bounds are enforced with redacted errors.                                                                                                                                                        | Bound exceeded returns typed error without raw argv/payloads; Unix duration coverage joins readers on every exit.                                                                                                                                                 | OpenCode #28 consumes this shared policy; file-backed Droid #29 does not invoke a provider subprocess.                                                             | Test fails if caps are soft or diagnostics leak argv/payloads.                                                                                | `feature/distill-clean-rebuild`      |
| `OSR-009`   | `library_ops_sync`                | Unknown or empty/disabled SyncRequest selections reject with zero side effects.                                                                                                                                                           | Typed `invalid_argument` / `sync_no_enabled_sources`; zero Sync Run and Activity rows.                                                                                                                                                                          | Operators cannot queue empty/invalid syncs.                                                                                                                        | Test fails if a Sync Run or Activity is inserted before validation.                                                                           | `feature/distill-clean-rebuild`      |
| `OSR-010`   | `library_ops_sync`                | One good Fixture Source plus one unavailable provider terminates as warning.                                                                                                                                                              | Run status `warning`; both source outcomes persisted; `sync_completed` (not `sync_failed`).                                                                                                                                                                     | Partial Source success is operational warning.                                                                                                                     | Test fails if mixed outcomes become `failed` or omit `sync_completed`.                                                                        | `feature/distill-clean-rebuild`      |
| `OSR-011`   | `library_ops_sync`                | One valid + one invalid Fixture candidate terminates as warning with both counters.                                                                                                                                                       | Accepted/successful/failed counters reflect both candidates; source status `warning`; `sync_completed` payload status warning.                                                                                                                                  | Partial candidate success is proven.                                                                                                                               | Test fails if the run is `failed`/`completed` or drops an outcome.                                                                            | `feature/distill-clean-rebuild`      |
| `OSR-012`   | `library_ops_sync`                | Bad Fixture manifest + enabled unavailable provider fails with no progress.                                                                                                                                                               | Run status `failed`; all source outcomes `failed`; `sync_failed` Activity.                                                                                                                                                                                      | Fatal no-progress is distinct from warning.                                                                                                                        | Test fails if the run is `warning`/`completed` or skips terminal Activity.                                                                    | `feature/distill-clean-rebuild`      |
| `OSR-013`   | `library_ops_sync`                | Cancel on terminal runs is idempotent; `sync_status(None)` with no runs is typed NotFound.                                                                                                                                                | Terminal cancel is a no-op; empty status returns `not_found` (not a raw SQLite error).                                                                                                                                                                          | Status/cancel edges are typed.                                                                                                                                     | Test fails if terminal cancel mutates state or empty status leaks SQLite.                                                                     | `feature/distill-clean-rebuild`      |
| `OSR-014`   | `library_ops_sync`                | Background lease heartbeat keeps a live long checkpoint active past the stale window.                                                                                                                                                     | Under `test-leases` short intervals, a held CandidateStarted remains `operations_status=active` beyond the stale threshold.                                                                                                                                     | Long candidates are not falsely marked stale.                                                                                                                      | Test fails if a live owner is classified stale without heartbeat loss.                                                                        | `feature/distill-clean-rebuild`      |
| `OSR-015`   | `library_ops_sync`                | Large provider stdin drains without deadlock and bounded process cleanup leaves no raw argv/output in diagnostics.                                                                                                                         | The bounded runner joins readers and cleans temporary process state after success, timeout, overflow, and malformed-export paths.                                                                                                                               | OpenCode/provider callers receive typed safe results within the shared subprocess policy.                                                                         | Test fails if large stdin blocks, readers leak, or diagnostics expose command/output data.                                                    | `feature/distill-clean-rebuild`      |
| `FIR-001`   | `library_fault_injection`         | Stage write before rename leaves only a staging partial; open reconciles it.                                                                                                                                                              | No Capture/CAS; one `.partial`; reopen removes it.                                                                                                                                                                                                              | Safe recovery without destructive repair.                                                                                                                          | Test fails if Capture is accepted or partial survives open.                                                                                   | `feature/distill-clean-rebuild`      |
| `FIR-002`   | `library_fault_injection`         | Rename before Capture acceptance leaves an orphan CAS blob repaired explicitly.                                                                                                                                                           | No Capture; one unreferenced blob; repair deletes it.                                                                                                                                                                                                           | Health classifies orphan; open does not delete it.                                                                                                                 | Test fails if Capture exists or open deletes the orphan.                                                                                      | `feature/distill-clean-rebuild`      |
| `FIR-003`   | `library_fault_injection`         | Capture insert before `capture_recorded` rolls back the shared SQLite transaction.                                                                                                                                                        | No Capture and no `capture_recorded` Activity.                                                                                                                                                                                                                  | Impossible mid-tx split is not simulated as durable.                                                                                                               | Test fails if half-committed Capture/Activity remains.                                                                                        | `feature/distill-clean-rebuild`      |
| `FIR-004`   | `library_fault_injection`         | After Capture+Activity before Attempt leaves incomplete Capture repaired via `capture_failed`.                                                                                                                                            | Capture + `capture_recorded` durable; no Attempt; repair appends `capture_failed` only.                                                                                                                                                                         | Health reports `incomplete_capture` until recovery Activity exists.                                                                                                | Test fails if a fake Attempt appears or Capture is lost.                                                                                      | `feature/distill-clean-rebuild`      |
| `FIR-005`   | `library_fault_injection`         | Pending Attempt before publish preserves last-good projection.                                                                                                                                                                            | New Capture + pending Attempt; prior generation/FTS unchanged.                                                                                                                                                                                                  | Repair fails the pending Attempt safely.                                                                                                                           | Test fails if projection mutates or pending becomes succeeded.                                                                                | `feature/distill-clean-rebuild`      |
| `FIR-006`   | `library_fault_injection`         | Facts/projection before FTS rolls back the publication transaction.                                                                                                                                                                       | Pending Attempt remains; last-good projection preserved.                                                                                                                                                                                                        | Mid-tx rows are not durable.                                                                                                                                       | Test fails if partial projection/FTS rows remain.                                                                                             | `feature/distill-clean-rebuild`      |
| `FIR-007`   | `library_fault_injection`         | After FTS before Attempt success/Activity rolls back publication.                                                                                                                                                                         | Pending Attempt remains; last-good projection preserved.                                                                                                                                                                                                        | Mid-tx rows are not durable.                                                                                                                                       | Test fails if Attempt succeeds or projection replaces.                                                                                        | `feature/distill-clean-rebuild`      |
| `FIR-008`   | `library_fault_injection`         | After `projection_replaced` before commit rolls back publication.                                                                                                                                                                         | Pending Attempt remains; last-good projection preserved.                                                                                                                                                                                                        | Activity without commit is not durable.                                                                                                                            | Test fails if `projection_replaced` persists without success.                                                                                 | `feature/distill-clean-rebuild`      |
| `TCC-001`   | `thin_cli_fixture_caller`         | CLI Fixture journey prints stable human and JSON results with exit `0`.                                                                                                                                                                   | Chosen temp home contains accepted Capture and Session Projection.                                                                                                                                                                                              | Human/JSON stdout includes source, sync, session, and health.                                                                                                      | Test fails if the CLI duplicates Library policy or invents unstable output.                                                                   | `feature/distill-clean-rebuild`      |
| `TCC-002`   | `thin_cli_fixture_caller`         | CLI usage and Library failures return documented exit codes `2` and `1`.                                                                                                                                                                  | No Capture is accepted for invalid Fixture roots.                                                                                                                                                                                                               | JSON stderr carries a stable error class.                                                                                                                          | Test fails if exit codes or error classes drift.                                                                                              | `feature/distill-clean-rebuild`      |
| `TCC-003`   | `thin_cli_fixture_caller`         | CLI `health` and `repair --confirm` print stable human/JSON with documented exits.                                                                                                                                                        | Repair without `--confirm` exits `2`; confirmed repair returns named actions.                                                                                                                                                                                   | Health statuses include staging/orphan/incomplete.                                                                                                                 | Test fails if repair skips confirmation or invents policy.                                                                                    | `feature/distill-clean-rebuild`      |
| `TCC-004`   | `thin_cli_multisource_caller`     | The real CLI drives Codex, Claude Code, OpenCode, and Droid through Source preferences, Sync, current-projection search/detail, Curation, `distill-session-jsonl-v1` export, Activity, and Operations. | Each isolated temporary home contains one accepted provider Capture, one current Session Projection, one curation mutation, and one published Export Artifact. | CLI JSON reports the provider kind, typed completed Sync, query/detail text, `train_ready` Curation, published export, and durable Activity/Operations evidence without provider-specific caller policy. | Test fails if a provider is only proven through Library internals, a caller path invents provider policy, or a healthy Source cannot complete the same product loop. | `feature/distill-clean-rebuild`      |
| `TCC-005`   | `thin_cli_multisource_caller`     | CLI projection/detail remains available after a file-backed provider root and OpenCode virtual export root are removed; a malformed configured provider remains isolated from a healthy Fixture Source. | Distill-owned Capture/Projection data remains queryable after source removal; mixed-source Sync completes as a warning with healthy Fixture acceptance and failed provider diagnostics. | CLI JSON contains no removed source paths or private tokens and preserves stable session/search output and typed warning detail. | Test fails if the CLI rereads a removed root, publishes redacted-path violations, or lets one failing Source abort its siblings. | `feature/distill-clean-rebuild`      |
| `TCC-006`   | `thin_cli_fixture_caller`         | CLI JSON Sync → Activity Capture discovery → `captures attempts` → same-process parser-version advance → `captures renormalize` → detail/search/Activity. | No new Capture; prior Attempts immutable; newer Attempt appended; Projection generation advances. | Attempt summaries expose parser identity/version, outcome, Fact count, and projection generation without SQL. | Test fails if callers invent Captures, mutate prior Attempts, or bypass public Library seams. | `feature/distill-clean-rebuild`      |
| `TCC-007`   | `thin_cli_multisource_caller`     | CLI Codex Capture renormalize succeeds after file-backed Source-root removal using Distill-owned bytes only. | Projection/search remain available; Attempt history appends a newer registered parser version. | CLI JSON omits removed roots and private tokens. | Test fails if renormalize rereads the removed root or loses Projection. | `feature/distill-clean-rebuild`      |
| `TCC-008`   | `thin_cli_source_detect_caller`   | CLI `sources detect` returns one typed result per `--request` with sibling isolation and redacted diagnostics. | Mixed Fixture ok/unhealthy plus disabled/unavailable siblings coexist in one response; exit `0`. | Human/JSON expose `status` / `error_class` / redacted `error_message` without path/token dumps. | Test fails if one bad Source suppresses siblings or diagnostics leak path fragments. | `feature/distill-clean-rebuild`      |
| `TCC-009`   | `thin_cli_source_detect_caller`   | CLI `sources detect` usage validation exits `2`; successful detection mutates neither Activity nor Sync Runs. | Empty `KIND=` root / blank request rejected before Library work; unknown kind remains a typed unhealthy result. | Activity stays empty and no Sync Run is created. | Test fails if detect appends Activity, invents Sync Runs, or treats unknown kind as a hard abort. | `feature/distill-clean-rebuild`      |
| `THC-001`   | `thin_tauri_host_fixture_caller`  | Host runner validates inputs, emits progress, and returns typed journey results.                                                                                                                                                          | Temp home matches Library Fixture outcomes.                                                                                                                                                                                                                     | Progress phases and source/sync/session/health are observed at the host seam.                                                                                      | Test fails if work runs without validation or errors lose their codes.                                                                        | `feature/distill-clean-rebuild`      |
| `THC-002`   | `thin_tauri_host_fixture_caller`  | Host health/repair validate home and require repair confirmation.                                                                                                                                                                         | Temp home health matches Library; denied repair is validation.                                                                                                                                                                                                  | Typed health/repair results reach the host seam.                                                                                                                   | Test fails if repair runs without confirm or leaks storage handles.                                                                           | `feature/distill-clean-rebuild`      |
| `THC-003`   | `thin_tauri_host_multisource_caller` | Host configures Codex, Claude Code, OpenCode, and Droid through typed Source preferences, then drives Sync, search/detail, curation, export, Activity, and Operations.                 | Each isolated temp home contains one accepted Capture, current Projection, curation mutation, published Export Artifact, and corresponding Activity/Operations rows.                                   | Public host runners return provider-neutral typed results for all four Source kinds.                                                                                | Test fails if Tauri adds provider policy, bypasses Library seams, or loses durable product-loop state.                                           | `feature/distill-clean-rebuild`      |
| `THC-004`   | `thin_tauri_host_multisource_caller` | Host provider isolation keeps Codex/OpenCode projections queryable after root removal and renders mixed-source failure as a redacted warning while Fixture succeeds.                  | Removed roots are not reread; the healthy Fixture Capture/Projection remains durable beside a failed provider Source.                                                                            | Sync terminates `warning`, healthy Source status is completed, and diagnostics omit source paths/content.                                                         | Test fails if warnings leak raw roots/payloads or a failed Source aborts healthy siblings.                                                       | `feature/distill-clean-rebuild`      |
| `THC-005`   | `thin_tauri_host_multisource_caller` | Host Capture Attempt history + renormalize after file-backed Codex root removal, with Fixture Sync evidence in the same home. | No new Capture; newer Attempt appended; Projection generation advances from Distill-owned bytes. | Host returns typed Attempt summaries and RenormalizeReport without path dumps. | Test fails if host rereads removed roots, invents Captures, or mutates prior Attempts. | `feature/distill-clean-rebuild`      |
| `THC-006`   | `thin_tauri_host_multisource_caller` | Unknown persisted Source kind rejects host renormalize without Attempt/Projection mutation. | Attempt history unchanged; typed `unknown_source_kind` with redacted message. | Error omits filesystem path dumps. | Test fails if unknown kind invents Attempts, mutates Projection, or leaks paths. | `feature/distill-clean-rebuild`      |
| `THC-007`   | `thin_tauri_host_source_detect_caller` | Host `detect_sources` validates home/requests, returns independent typed results, redacts diagnostics, and appends no Activity. | Mixed Fixture ok/unhealthy plus disabled/unavailable siblings coexist; empty/blank requests are validation errors. | Host remains a read-only bridge over `Library::detect_sources`. | Test fails if host invents provider policy, aborts siblings, mutates Activity, or leaks path fragments. | `feature/distill-clean-rebuild`      |
| `TRC-001`   | `thin_react_fixture_caller`       | React first-run UI renders idle/running/success/error plus source/sync/session/health via one typed bridge fake.                                                                                                                          | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | Panels and status reflect bridge results/errors only.                                                                                                              | Test fails if the UI reaches ambient filesystem/process/SQLite APIs.                                                                          | `feature/distill-clean-rebuild`      |
| `TRC-002`   | `thin_react_fixture_caller`       | Production renderer bridge invokes the exact Tauri command shape and cleans up asynchronous event listeners.                                                                                                                              | N/A — bridge exposes typed values only.                                                                                                                                                                                                                         | Snake-case Rust parameters receive the required camel-case invoke key; unmount cannot leak a late listener.                                                        | Test fails if production IPC differs from the fake or cleanup races registration.                                                             | `feature/distill-clean-rebuild`      |
| `TRC-003`   | `thin_react_fixture_caller`       | React health/repair surface requires confirmation and renders typed issues/actions.                                                                                                                                                       | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | Repair stays disabled until confirm; bridge-only data is shown.                                                                                                    | Test fails if repair can run without confirm or ambient authority appears.                                                                    | `feature/distill-clean-rebuild`      |
| `TRC-004`   | `thin_react_multisource_caller`  | React persists Source kind/root/enabled drafts through the typed bridge, starts Sync with only selected kinds, and renders safe mixed-source warning details.                     | N/A — renderer has no storage authority; the bridge receives typed preference calls and selected Source filters.                                                                                     | Accessible Source controls serialize Codex/OpenCode roots; warning state is visible without a source-content panel or leaked payload.                              | Test fails if disabled preferences are not durable, selected kinds drift, or renderer introduces provider parsing/ambient authority.              | `feature/distill-clean-rebuild`      |
| `TRC-005`   | `thin_react_fixture_caller`      | React session detail loads Attempt history via Activity Capture discovery and renormalize through the typed bridge with idle/loading/ready/empty/warning/error states. | N/A — renderer has no storage authority; Capture ids come from bridge Activity results only. | Attempt list and renormalize report/status stay bridge-only; no parser-version preference UI. | Test fails if UI invents Capture ids, reaches ambient APIs, or skips explicit loading/error states. | `feature/distill-clean-rebuild`      |
| `TRC-006`   | `thin_react_source_detect_caller` | React Source detection control invokes `detectSources` through the typed bridge and renders per-Source status with idle/loading/ready/empty/warning/error states. | N/A — renderer has no storage authority; requests come from Source preference drafts only. | Bridge invoke uses `detect_sources_command` with camel-case args; UI never reaches ambient FS/process APIs. | Test fails if detection invents provider policy, skips bridge invoke, or gains ambient authority. | `feature/distill-clean-rebuild`      |
| `A11Y-001`  | `thin_react_a11y_visual`          | Keyboard navigation covers search, workflow lanes, session selection, curation, dialogs, Sync Run cancellation, export cancellation, and legacy migration submission.                                                                   | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | Native controls submit/activate by keyboard, the migration source field accepts DOM `Enter` and WebKitGTK `Return`, and cancelled requests return focus to their initiating control. | Test fails if a required action is pointer-only or focus is lost after cancellation.                                                          | `feature/distill-clean-rebuild`      |
| `A11Y-002`  | `thin_react_a11y_visual`          | Renderer exposes semantic names/groups, live status/progress, busy panels, alerts, and a focus-contained repair dialog.                                                                                                                   | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | Screen-reader navigation has named landmarks and actionable status announcements.                                                                                  | Test fails if status is silent, controls are unnamed, or dialog focus escapes.                                                                | `feature/distill-clean-rebuild`      |
| `A11Y-003`  | `thin_react_a11y_visual`          | Focus indicators, contrast tokens, reduced motion, disabled contrast, and 200% text scaling remain usable.                                                                                                                                | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | Labeled controls remain discoverable at 200% root text size without motion dependence.                                                                             | Test fails if CSS loses focus visibility, relies on opacity, or drops labeled controls at scale.                                              | `feature/distill-clean-rebuild`      |
| `A11Y-004`  | `thin_react_a11y_visual`          | Deterministic renderer evidence covers first-run, loading, refreshing, empty, populated, warning, error, cancelled, migration, and export states.                                                                                         | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | State markers and visible copy remain stable for review and regression checks.                                                                                     | Test fails if a major state is unrepresented or rows disappear during refresh.                                                                | `feature/distill-clean-rebuild`      |
| `A11Y-005`  | `thin_react_a11y_visual`          | Post-build renderer smoke runs the a11y/state suites; macOS and Ubuntu packaged smokes prove Accessibility/AT-SPI dialog focus/cancellation state; packaged screen-reader speech remains an explicit human gate.                          | N/A — renderer has no storage authority.                                                                                                                                                                                                                        | `a11y:smoke` proves the renderer seam; `npm run desktop:smoke:macos` proves AX focus enters `Confirm destructive repair`, Tab stays contained, Escape closes, and focus returns to `Repair library`; `node apps/distill-desktop/scripts/linux-package-smoke.mjs` proves the installed Linux focus containment/return; the human checklist records VoiceOver/Narrator evidence. | Test fails if a renderer-only or packaged focus-state run is presented as VoiceOver/Narrator verification.                                    | `feature/distill-clean-rebuild`      |
| `PKG-001`   | `macos_packaged_smoke`            | Build the macOS Tauri bundle with documented identity, icon, minimum OS, and capability source.                                                                                                                                           | `.app` metadata reports `dev.distill.desktop`, `Distill`, macOS 12.0 minimum, checked-in `icon.icns`, and only `core:event:default`; local signing is ad-hoc/unsigned.                                                                                          | The package is inspectable without claiming Developer ID, hardened runtime, notarization, or ticket stapling.                                                      | Test fails if metadata drifts, the icon is absent, or a broader capability appears.                                                           | `feature/distill-clean-rebuild`      |
| `PKG-002`   | `macos_packaged_smoke`            | Launch a clean packaged app and complete Fixture sync, search, detail, curation, export, quit/relaunch, and artifact checks.                                                                                                              | A temporary chosen home contains `distill.db` and a JSONL export; restart preserves the artifact; Fixture source files remain unchanged.                                                                                                                        | macOS Accessibility automation records a real packaged renderer journey.                                                                                           | Test fails if the app cannot launch, a required control is inaccessible, export is absent, or restart changes the artifact set.               | `feature/distill-clean-rebuild`      |
| `PKG-003`   | `macos_packaged_smoke`            | Prove packaged writes stay within the chosen Distill home and do not mutate the Fixture source root.                                                                                                                                      | All observed home artifacts are descendants of the requested home; the before/after Fixture file set is identical.                                                                                                                                              | Runtime containment is evidenced without treating it as application encryption or secure deletion.                                                                 | Test fails if a packaged write escapes the home or source Fixture files change.                                                               | `feature/distill-clean-rebuild`      |
| `PKG-004`   | `macos_packaged_smoke`            | Seed hermetic Codex/Claude/OpenCode/Droid roots and drive Detect Sources + Start Sync Run before the Fixture search/detail/curation/export journey.                                                                                      | Sync completes with Fixture + hermetic providers; curated Fixture export and restart still succeed; provider roots remain unchanged.                                                                                                                             | Packaged Accessibility drives existing bridge-only Source preference/Detect/Sync controls; OpenCode uses `{root}/bin/opencode` stub only.                          | Test fails if Detect/Sync controls are inaccessible, Sync never reaches success, or hermetic roots are mutated.                               | `feature/distill-clean-rebuild`      |
| `PKG-005`   | `macos_packaged_smoke`            | Packaged Detect Sources keeps a missing Codex sibling isolated and redacted while configured siblings remain observable.                                                                                                                 | UI shows `Status: warning` and `codex: unhealthy`; `fixture: ok`, `claude_code: unavailable`, `opencode: ok`, and `droid: ok` remain visible without leaking the Detect sibling secret token in accessible names.                                               | Sibling isolation is packaged AX evidence only; Claude Code `unavailable` is expected when no host executable is installed. | Test fails if Detect aborts siblings, omits warning/status rows, or leaks the secret fragment.                                                 | `feature/distill-clean-rebuild`      |
| `PKG-006`   | `macos_packaged_smoke`            | After Fixture search/detail, packaged AX drives Load Attempt history and same-Capture Renormalize Capture.                                                                                                                               | Attempt status/list and renormalize report expose Capture id, Attempt id, outcome, and parser id/version; Fixture roots remain unchanged.                                                                                                                       | AX run is manual-required when the packaged window is not exposed by System Events; no parser-version preference UI or host-provider claim. | Test fails if the existing bridge-only controls cannot be reached or the retry report/list is missing.                                      | `feature/distill-clean-rebuild`      |
| `PKG-007`   | `macos_packaged_smoke`            | Seed a temporary host/CLI-shaped synthetic legacy Electron home and drive Import legacy home through the existing bridge-only migration panel.                                                                                           | Report shows `ok: true`, expected captures/sessions counts, `reused: false`; migrated session is searchable; before/after SHA-256 of the legacy home (including `distill.db-wal`/`distill.db-shm`) matches.                                                      | AX run is manual-required when System Events cannot expose the packaged window; no live-user-home, Electron-product edit, signing, or screen-reader claim. | Test fails if migration controls are unreachable, report fields drift, the migrated session is undiscoverable, or the legacy home mutates. | `feature/distill-clean-rebuild`      |
| `LPKG-001`  | `linux_packaged_smoke`            | Build Ubuntu `.deb` and AppImage artifacts with documented WebKitGTK/GTK runtime dependencies and the events-only capability source.                                                                                                      | `.deb` and `.AppImage` exist; Debian metadata declares `libwebkit2gtk-4.1-0` and GTK 3 alternatives; capability source remains `["core:event:default"]`.                                                                                                        | CI records package names and the installed host path without claiming a signed store artifact.                                                                     | Test fails if either artifact is missing, runtime dependencies are absent, or capabilities broaden.                                           | `feature/distill-clean-rebuild`      |
| `LPKG-002`  | `linux_packaged_smoke`            | Install the `.deb` on Ubuntu and complete Fixture sync, search, detail, train curation, export, quit/relaunch, and artifact checks under Xvfb/dbus.                                                                                       | `/usr/bin/distill-desktop` creates `distill.db` and a non-empty curated train JSONL; restart preserves the home artifact set.                                                                                                                                   | CI drives the installed binary rather than an unpackaged release host.                                                                                             | Test fails if install/launch/UI journey/restart/export fails.                                                                                 | `feature/distill-clean-rebuild`      |
| `LPKG-003`  | `linux_packaged_smoke`            | Prove the installed Linux host leaves Fixture files unchanged and writes only within the chosen home/Fixture temp roots.                                                                                                                  | Fixture paths and SHA-256 contents match before/after; new temp-parent files are constrained to chosen roots.                                                                                                                                                   | Runtime containment is evidenced without claiming privacy, encryption, or secure deletion.                                                                         | Test fails if a packaged write escapes the chosen roots or Fixture content changes.                                                           | `feature/distill-clean-rebuild`      |
| `LPKG-004`  | `linux_packaged_smoke`            | Seed hermetic Codex/Claude/OpenCode/Droid roots and drive Detect Sources + Start Sync Run before the Fixture search/detail/curation/export journey under Xvfb.                                                                             | Installed host Sync completes; curated Fixture export and restart still succeed; hermetic provider roots remain unchanged.                                                                                                                                       | AT-SPI/`xdotool` drive existing bridge-only Source preference/Detect/Sync controls; OpenCode uses `{root}/bin/opencode` stub only.                                 | Test fails if Detect/Sync controls are inaccessible, Sync never reaches success, or hermetic roots are mutated.                               | `feature/distill-clean-rebuild`      |
| `LPKG-005`  | `linux_packaged_smoke`            | Installed Detect Sources keeps a missing Codex sibling isolated and redacted while configured siblings remain observable.                                                                                                                | UI shows `Status: warning` and `codex: unhealthy`; `fixture: ok`, `claude_code: unavailable`, `opencode: ok`, and `droid: ok` remain visible without leaking the Detect sibling secret token in accessible names.                                               | Sibling isolation is packaged AT-SPI evidence only; Claude Code `unavailable` is expected when no host executable is installed. | Test fails if Detect aborts siblings, omits warning/status rows, or leaks the secret fragment.                                               | `feature/distill-clean-rebuild`      |
| `LPKG-006`  | `linux_packaged_smoke`            | After Fixture search/detail, installed AT-SPI/`xdotool` drives Load Attempt history and same-Capture Renormalize Capture.                                                                                                                | Attempt status/list and renormalize report expose Capture id, Attempt id, outcome, and parser id/version; Fixture roots remain unchanged and the retained curation/export/restart journey still passes.                                                        | Installed-host AT-SPI evidence only; no screen-reader, parser-version preference, or host-provider claim. | Test fails if the existing bridge-only controls cannot be reached or the retry report/list is missing.                                    | `feature/distill-clean-rebuild`; exact-head Ubuntu [29245798595](https://github.com/AustinKelsay/distill/actions/runs/29245798595) green |
| `LPKG-007`  | `linux_packaged_smoke`            | Seed a temporary host/CLI-shaped synthetic legacy Electron home and drive Import legacy home through the existing bridge-only migration panel before Detect/Sync.                                                                        | Report shows `ok: true`, expected captures/sessions counts, `reused: false`; migrated session is searchable; before/after SHA-256 of the legacy home (including sidecars) matches; retained Fixture journey still passes.                                           | Installed-host AT-SPI evidence plus a smoke-only Vite-built renderer DOM `requestSubmit` route (`VITE_DISTILL_SMOKE_DOM_ACTIVATE=1` only in the package workflow); shipped CSP remains unchanged; hermetic temp legacy home only; no live-user-home, Electron-product edit, screen-reader, or host-provider claim. | Test fails if migration controls are unreachable, report fields drift, the migrated session is undiscoverable, the legacy home mutates, or the retained journey regresses. | `feature/distill-clean-rebuild`; pre-route exact-head [29265217362](https://github.com/AustinKelsay/distill/actions/runs/29265217362) stops at `Migration status: idle`; validate the package-native route before promotion |
| `SCALE-001` | `library_scale_budgets`           | Deterministic synthetic corpus seeds the public Library seam at the documented Session/message/logical-home targets.                                                                                                                     | The bounded smoke proves corpus counts and API wiring; the scheduled full run uses 25,000 Sessions, 1,000,000 current-projection messages, and at least 10 GiB logical home content.                                                                             | JSON evidence records corpus counts, logical size, host, and whether the full benchmark or bounded smoke ran.                                                     | Test fails if the generator is nondeterministic, counts drift, or the full benchmark silently masquerades as a small smoke.                    | `feature/distill-clean-rebuild`      |
| `SCALE-002` | `library_scale_budgets`           | First page, search page, first detail slice, and one curation mutation meet warm p95 budgets of 150/150/150/100 ms on the recorded host; cold samples are reported separately.                                                            | N/A — read/curation operations use the public Library seam over the synthetic home.                                                                                                                                                                             | JSON evidence includes operation, cold/warm class, sample count, p50/p95, budget, corpus counts/size, and machine.                                                 | Test fails with actionable p95 evidence when a scheduled full run misses a budget; the small PR smoke proves wiring only.                     | `feature/distill-clean-rebuild`      |
| `SCALE-003` | `library_scale_budgets`           | Sync/export progress callbacks remain within a 500 ms maximum observed gap while multiple safe checkpoints advance.                                                                                                                       | Durable run/export state remains internally consistent; no mid-transaction interruption is claimed.                                                                                                                                                             | Library-only progress evidence reports the largest gap and checkpoint.                                                                                             | Test fails if boundary cadence exceeds 500 ms without a documented heartbeat variant.                                                         | `feature/distill-clean-rebuild`      |
| `SCALE-004` | `library_scale_budgets`           | Sync/export cancellation is observed at the next safe checkpoint within 1 s and reports the checkpoint/ack latency.                                                                                                                       | Current candidate/transaction may finish; terminal state is cancelled without misleading publication.                                                                                                                                                           | Public Library result and JSON timing evidence agree.                                                                                                              | Test fails if acknowledgement exceeds 1 s or cancellation claims mid-transaction interruption.                                                | `feature/distill-clean-rebuild`      |
| `EP-001`    | `library_export_publication`      | Preview and publish share train/holdout eligibility from current manual curation.                                                                                                                                                         | Exclude, sensitive, conflicting, and favorite-only sessions are omitted; approved sessions are included.                                                                                                                                                        | Preview returns deterministic eligible identities and omission reasons without side effects.                                                                       | Test fails if preview and publish diverge or non-approved sessions appear.                                                                    | `feature/distill-clean-rebuild`      |
| `EP-002`    | `library_export_publication`      | JSONL payload uses current projection metadata, ordered messages, manual labels/tags, and canonical turn pairs.                                                                                                                           | Golden lines preserve timestamps/kinds/metadata and consecutive-user replacement.                                                                                                                                                                               | Published bytes are deterministic and checksum-verifiable.                                                                                                         | Test fails if raw history, non-manual curation, or stale projection rows leak into export.                                                    | `feature/distill-clean-rebuild`      |
| `EP-003`    | `library_export_publication`      | Export publication reaches `published` only after same-volume rename and matching SQLite bookkeeping.                                                                                                                                     | `exports` row, final file, checksum/counts, and `export_written` agree.                                                                                                                                                                                         | Reopen can classify incomplete states without inventing success.                                                                                                   | Test fails if DB commits before rename or reports a misleading path.                                                                          | `feature/distill-clean-rebuild`      |
| `EP-004`    | `library_export_publication`      | Cancellation, temporary-write, committed-before-rename, and rename-before-finalization faults recover safely.                                                                                                                             | Terminal `cancelled`/`failed_publish` or verified completion has no orphaned misleading state.                                                                                                                                                                  | Reopen removes only disposable temp files and preserves referenced output.                                                                                         | Test fails if a crash leaves an untracked published file or deletes a valid artifact.                                                         | `feature/distill-clean-rebuild`      |

Executable Library Fixture suite: `crates/distill-library/tests/library_fixture_tracer.rs`.
Executable Library Codex suite: `crates/distill-library/tests/library_codex_source.rs` (adapter unit corpus runs with the Library package tests).
Executable Library Claude suite: `crates/distill-library/tests/library_claude_source.rs` (adapter unit corpus runs with the Library package tests).
Executable Library OpenCode suite: `crates/distill-library/tests/library_opencode_source.rs` (virtual CLI, replay, redaction, timeout/overflow/malformed contracts; adapter unit corpus runs with the Library package tests).
Executable Library Droid suite: `crates/distill-library/tests/library_droid_source.rs` (default/override roots, mixed parsing, sidecar metadata, typed failures, progress redaction, and replay; recursive discovery/dedupe and synthetic identity assertions run in the adapter unit corpus with the Library package tests).
Executable Library Attempt Retry suite: `crates/distill-library/tests/library_attempt_retry.rs`.
Executable Library Health/Repair suite: `crates/distill-library/tests/library_health_repair.rs`.
Executable Library Fault Injection suite: `crates/distill-library/tests/library_fault_injection.rs` (`cargo test -p distill-library --features test-faults`).
Executable Library Ops/Sync suite: `crates/distill-library/tests/library_ops_sync.rs` (heartbeat contract requires `--features test-leases`).
Executable Library Export suite: `crates/distill-library/tests/library_export_publication.rs` (`cargo test -p distill-library --test library_export_publication --features test-faults`).
Executable Library Legacy Import suite: `crates/distill-library/tests/library_legacy_import.rs`.
Executable privacy-hardening suites: `crates/distill-library/tests/library_hostile_inputs.rs`, `crates/distill-library/tests/library_opencode_source.rs`, `crates/distill-library/tests/library_ops_sync.rs`, `apps/distill-desktop/src-tauri/tests/host_hostile_inputs.rs`, and `apps/distill-desktop/src/bridge.test.ts`.
Executable CLI suite: `crates/distill-cli/tests/cli_fixture_journey.rs`.
Executable Tauri host suite: `apps/distill-desktop/src-tauri/tests/host_fixture_journey.rs`.
Executable React suites: `apps/distill-desktop/src/App.test.tsx`, `apps/distill-desktop/src/bridge.test.ts`, `apps/distill-desktop/src/App.a11y.test.tsx`, `apps/distill-desktop/src/App.states.test.tsx`, and `apps/distill-desktop/src/styles.a11y.test.ts`.
Executable macOS package suite: `apps/distill-desktop/scripts/macos-package-smoke.mjs` (Darwin-only; builds are invoked with `npm run desktop:package:macos`, and the smoke uses a local ad-hoc/unsigned `.app`; it proves packaged AX repair-dialog focus containment/return as Accessibility focus state only, plus hermetic multi-Source Detect/Start Sync via temporary roots and a local OpenCode stub; `PKG-006` Attempt-history/renormalize and `PKG-007` hermetic legacy Electron-home import remain explicit manual AX checklists when System Events exposes the window; it does not claim crash recovery, privacy, scale, export atomicity, VoiceOver, host-installed providers, live-user legacy homes, Electron-product edits, or signed/notarized release packaging).
Executable Linux package suite: `apps/distill-desktop/scripts/linux-package-smoke.mjs` (Ubuntu CI-only; the workflow installs the `.deb` and drives the installed host under Xvfb/dbus with `xdotool`; it proves AT-SPI repair-dialog focus containment/return as accessible focus state only, plus hermetic multi-Source Detect/Start Sync via temporary roots and a local OpenCode stub, the bridge-only Attempt-history/same-Capture renormalize journey (`LPKG-006`), and the bridge-only hermetic legacy Electron-home import journey (`LPKG-007`) with a smoke-only Vite-built renderer `requestSubmit` fallback; it does not claim crash recovery, privacy, scale, export atomicity, host-installed providers, live-user legacy homes, Electron-product edits, or screen-reader coverage).
Executable packaged hermetic fixture seam: `apps/distill-desktop/scripts/packaged-hermetic-multisource.mjs` and `apps/distill-desktop/scripts/packaged-hermetic-legacy-home.mjs` (`npm --prefix apps/distill-desktop run test:hermetic-fixtures`).
Executable Library scale suite: `crates/distill-library/tests/library_scale_budgets.rs` (bounded smoke by default; full logical 10 GiB benchmark requires `DISTILL_SCALE_BENCH=1` and `--ignored`).

## Cutover evidence registry

The scenario table above remains the concise normative contract. The companion
[cutover evidence registry](contract-scenario-evidence.md) supplies the #37 audit
fields for every scenario ID: owning spec clause, contract family/seam, fixture,
executable test symbol, expected result (by reference to the scenario row), durable
and Activity effect, supported platforms, status, and last evidence. A scenario is
not considered cutover-complete unless it has an explicit registry status; legacy
Electron rows are retained as `legacy-baseline` evidence rather than deleted.

## Expected DB State Guidance

For every executable scenario, the test implementation should explicitly assert:

- capture row count and status changes
- capture content ref presence where required
- session row stability or replacement
- message and artifact row membership in the current projection
- activity event coverage
- job/log summaries when relevant

## Expected UI / Query Outcome Guidance

For every executable scenario, the test implementation should explicitly assert:

- session list title and preview behavior where relevant
- session detail transcript correctness
- session detail projection metadata and provenance visibility
- artifact visibility and linkage
- search result freshness after re-imports
- export payload correctness

## Branch Mapping Rule

Scenarios become executable in the first branch that claims their acceptance criteria. A future implementation branch is not complete until its mapped scenarios are executable and passing.
