{
  "name": "distill",
  "private": true,
  "workspaces": [
    "apps/distill-electron"
  ],
  "scripts": {
    "start": "npm run start --workspace distill-electron",
    "build": "npm run build --workspace distill-electron",
    "doctor": "npm run doctor --workspace distill-electron",
    "import": "npm run import --workspace distill-electron",
    "export": "npm run export --workspace distill-electron",
    "test": "npm run test --workspace distill-electron",
    "desktop:check": "cargo check -p distill-desktop"
  }
}
