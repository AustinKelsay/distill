# Deep Distill Library crate: public product seam over SQLite, CAS, and SourceAdapters.

[package]
name = "distill-library"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Distill local conversation library (capture, normalize, query, health)"

[features]
default = []
## Test-only ingest fault injection. Absent from production default builds.
test-faults = []
## Test-only Sync lease/heartbeat timing overrides. Absent from production default builds.
test-leases = []

[dependencies]
chrono = { workspace = true }
hex = { workspace = true }
rusqlite = { workspace = true }
semver = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
chrono = { workspace = true }
hex = { workspace = true }
rusqlite = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
tempfile = { workspace = true }
